# Copy this file to .groq.properties in this folder and paste your key from https://console.groq.com/keys
# Run Maven from back/ so Spring finds the file (optional:file:.groq.properties).
groq.api.key=
